# 发布 Aura Canvas v0.2.0

## 🚀 发布步骤

### 1. Git 提交和标签

```bash
# 1. 提交所有更改
git add .
git commit -m "Release v0.2.0: Dual Grid Controls, improved style system, bug fixes"

# 2. 创建版本标签
git tag -a v0.2.0 -m "Release version 0.2.0"

# 3. 推送到远程仓库（包括标签）
git push origin main
git push origin v0.2.0
```

### 2. 创建 GitHub Release

1. 访问你的 GitHub 仓库
2. 点击 "Releases" → "Create a new release"
3. 选择标签: `v0.2.0`
4. Release title: `Aura Canvas v0.2.0`
5. 复制以下内容到描述框：

```markdown
# Aura Canvas v0.2.0

🎉 **重大更新**：双重网格控制、改进的样式系统和多项bug修复

## 🆕 新功能

### 双重网格配置控制
- **Apply Grid Config**: 用于全局设置（列数、间距、最小/最大宽度）
- **Apply Modify**: 用于调整选定section的间距（padding、gap）
- 分离按钮避免意外创建section，允许精确修改

### 改进的样式选择和预览
- 选择样式时自动显示第一张数据卡预览
- 无需在预览面板中手动选择卡片
- 实时样式渲染，缺失缩略图时有适当的fallback
- 通过manifest系统支持style文件夹中的所有JSON样式文件

### 增强的样式文件管理
- 新增 `style/manifest.json` 用于集中式样式文件配置
- 支持用户自定义JSON卡片样式
- 自动加载所有定义的样式文件
- 正确处理有/无 `cardStyle` 定义的样式

## 🐛 Bug修复
- ✅ 修复Import HTML和Tally表单按钮需要双击的问题
- ✅ 修复重复的事件监听器绑定（使用cloneNode技术）
- ✅ 修复样式下拉菜单中的重复条目（删除重复文件）
- ✅ 修复Grid Config自动创建不必要的section
- ✅ 修复Minimal Testimonial Card缩略图显示
- ✅ 修复样式预览需要右侧面板选择卡片的问题
- ✅ 修复自定义JSON文件不加载（添加manifest系统）

## 📥 下载和安装

### 快速开始
1. 下载下方的 `aura-canvas-v0.2.0.zip`
2. 解压到任意目录
3. 双击 `editor.html` 或运行 `启动服务器.bat`

### 系统要求
- 现代浏览器（Chrome 90+, Firefox 88+, Safari 14+, Edge 90+）
- 推荐使用本地服务器（Live Server等）

## 📄 完整文档
- [详细更新说明](RELEASE_v0.2_README.md)
- [使用文档](README.md)

## ⬆️ 从 v0.1 升级
直接打开新版 `editor.html` - 向后兼容，localStorage中的数据会保留。
```

6. 上传以下文件作为附件：
   - 整个项目打包成 `aura-canvas-v0.2.0.zip`

### 3. 本地打包（无GitHub情况下）

如果不使用GitHub，创建一个本地zip包：

```bash
# 运行打包脚本
.\create-release-package.bat
```

生成的文件：
- `releases/aura-canvas-v0.2.0.zip` - 完整发布包
- 可直接分享给客户

---

## 📦 发布包内容

release包应包含：
- ✅ editor.html
- ✅ css/editor.css
- ✅ js/*.js (所有JavaScript文件)
- ✅ style/*.json (样式文件)
- ✅ README.md
- ✅ RELEASE_v0.2_README.md
- ✅ package.json
- ✅ VERSION
- ✅ 启动服务器.bat

**不包含**：
- ❌ .git/ (Git仓库文件)
- ❌ node_modules/ (如果存在)
- ❌ .gitignore
- ❌ 开发相关的文档（设计提示等）

---

## 🔗 客户下载说明

客户可通过以下方式获取：

### 方式1: GitHub Release（推荐）
1. 访问: `https://github.com/你的用户名/aura-canvas/releases`
2. 找到 `v0.2.0`
3. 下载 `aura-canvas-v0.2.0.zip`

### 方式2: 直接下载链接
```
https://github.com/你的用户名/aura-canvas/archive/refs/tags/v0.2.0.zip
```

### 方式3: Git Clone
```bash
git clone https://github.com/你的用户名/aura-canvas.git
cd aura-canvas
git checkout v0.2.0
```
